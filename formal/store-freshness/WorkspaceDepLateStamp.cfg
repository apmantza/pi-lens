\* expect: pass
\* module: StoreFreshness
\* The code (#3505): the workspace-diagnostics cache entry's scannedAt is taken per file before its read (runWorkspaceDiagnostics noteScanStat; a pull, before its request; lsp_diagnostics, before its stat), and isEntryFresh compares imports against it. Was violated; the pre-fix stamp is MutWorkspaceDepLateStamp.
CONSTANTS
    Writes = 2
    SizeMode = "any"
    MaxTime = 6
    Tol = 2
    Gran = 1
    Skew = 1
    MinReadDelay = 0
    Stamp = "start"
    Gate = "mtime"
    ForceHash = FALSE
    SizeOnly = FALSE
    StatAt = "before"
    SizeKey = TRUE
    Binding = FALSE
    BindingAt = "read"
    MaxServes = 2
    Latch = FALSE
    AllowRegress = FALSE
    ServerLag = FALSE
SPECIFICATION Spec
INVARIANTS NoStaleServeBeyondTol NoSpuriousDemotion
CHECK_DEADLOCK FALSE
