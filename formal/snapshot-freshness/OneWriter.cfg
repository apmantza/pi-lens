\* expect: pass
\* module: SnapshotFreshness
\* One process: the seq names a tree state. It held before #3511 too (LogAlloc and CompleteStamp FALSE); this config checks the code as it is.
CONSTANTS
    Procs = {p1}
    MaxEdits = 4
    LogAlloc = TRUE
    CompleteStamp = TRUE
SPECIFICATION Spec
INVARIANTS FreshMeansComplete BoundedReplayExact FreshForOwnRuntime
CHECK_DEADLOCK FALSE
