\* expect: violated FreshMeansComplete
\* module: SnapshotFreshness
\* Fix minus the completeness stamp: a unique seq above a sibling's entry the runtime never folded.
CONSTANTS
    Procs = {p1, p2}
    MaxEdits = 4
    LogAlloc = TRUE
    CompleteStamp = FALSE
SPECIFICATION Spec
INVARIANTS FreshMeansComplete BoundedReplayExact FreshForOwnRuntime
CHECK_DEADLOCK FALSE
