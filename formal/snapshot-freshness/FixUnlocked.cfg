\* expect: pass
\* module: SnapshotFreshness
\* The code with the change-log lock timing out (review round 2, R2-F1): an unlocked append reads the log max and appends in two steps, tagged unlocked; a reader distrusts a snapshot with an unlocked entry after its fold point. Two processes, timed-out seeds.
CONSTANTS
    Procs = {p1, p2}
    MaxEdits = 3
    LogAlloc = TRUE
    CompleteStamp = TRUE
    SeqCAS = TRUE
    ColdSeeds = TRUE
    UnlockedEdits = TRUE
    ReaderRule = "tag"
    NeverSeqMutant = FALSE
    RefuseAllMutant = FALSE
    AlwaysIncompleteMutant = FALSE
SPECIFICATION Spec
INVARIANTS FreshMeansComplete BoundedReplayExact FreshForOwnRuntime NewestSaveLands IncompleteOnlyWhenMissing
CHECK_DEADLOCK FALSE
