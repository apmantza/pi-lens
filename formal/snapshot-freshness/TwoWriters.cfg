\* expect: pass
\* module: SnapshotFreshness
\* Two processes: the seq is allocated from the log under its lock, and a runtime that missed a sibling's logged edit stamps never-fresh (#3511; violated FreshMeansComplete before, with LogAlloc and CompleteStamp FALSE).
CONSTANTS
    Procs = {p1, p2}
    MaxEdits = 2
    LogAlloc = TRUE
    CompleteStamp = TRUE
SPECIFICATION Spec
INVARIANTS FreshMeansComplete
CHECK_DEADLOCK FALSE
