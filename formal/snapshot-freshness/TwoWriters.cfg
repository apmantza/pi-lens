\* expect: pass
\* module: SnapshotFreshness
\* The code, two processes: the seq is allocated from the log under its lock, and a runtime that missed a sibling's logged edit stamps its snapshot incomplete (#3511; violated FreshMeansComplete before, with LogAlloc and CompleteStamp FALSE).
CONSTANTS
    Procs = {p1, p2}
    MaxEdits = 2
    LogAlloc = TRUE
    CompleteStamp = TRUE
    SeqCAS = TRUE
    ColdSeeds = TRUE
    NeverSeqMutant = FALSE
    RefuseAllMutant = FALSE
    AlwaysIncompleteMutant = FALSE
SPECIFICATION Spec
INVARIANTS FreshMeansComplete
CHECK_DEADLOCK FALSE
