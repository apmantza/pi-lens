\* expect: pass
\* module: DispatchPipeline
\* Fixed code (#3506), parallel tools: an agent edit cannot land inside the fixer's before/after window, so nothing the pipeline reports as its autofix holds an agent edit (checked without the lost-update invariant).
CONSTANTS
    Edits = 2
    Parallel = TRUE
    Orphan = FALSE
    ImmediateEdits = {1}
    ClaimGap = FALSE
    InFlightDedupe = TRUE
    WidgetGuard = TRUE
    FixRecord = FALSE
    FixClear = FALSE
    FixTomb = FALSE
    FixQueue = TRUE
    FixQueueRefresh = TRUE
    FixReToken = TRUE
SPECIFICATION Spec
INVARIANTS NoForeignAttribution
CHECK_DEADLOCK FALSE
