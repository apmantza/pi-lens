\* expect: violated WidgetNewest
\* module: DispatchPipeline
\* Fix mutant: the queue held through the refresh, but the refreshed content keeps the handler's writeIndex; a queued fixer that fixes a newer revision analyses it under an older token.
CONSTANTS
    Edits = 3
    Parallel = TRUE
    Orphan = FALSE
    ImmediateEdits = {1}
    ClaimGap = FALSE
    InFlightDedupe = TRUE
    WidgetGuard = TRUE
    FixRecord = FALSE
    FixClear = FALSE
    FixTomb = FALSE
    FixQueue = TRUE
    FixQueueRefresh = TRUE
    FixReToken = FALSE
    WriterBound = FALSE
    FixHoldWriter = TRUE
SPECIFICATION Spec
INVARIANTS NoLostEdit NoForeignAttribution WidgetNewest
CHECK_DEADLOCK FALSE
