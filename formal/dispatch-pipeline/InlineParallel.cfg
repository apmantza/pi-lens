\* expect: pass
\* module: DispatchPipeline
\* Fixed code (#3507), pi's default parallel tool execution: two same-file edits' pipelines overlap, and the handler records and clears the inline-blocker record through one per-path WriteOrderingGuard on the dispatch writeIndex (compare on record, compare on clear, token kept across a clear).
CONSTANTS
    Edits = 2
    Parallel = TRUE
    Orphan = FALSE
    ImmediateEdits = {}
    ClaimGap = FALSE
    InFlightDedupe = TRUE
    WidgetGuard = TRUE
    FixRecord = TRUE
    FixClear = TRUE
    FixTomb = TRUE
    FixQueue = FALSE
    FixQueueRefresh = FALSE
    FixReToken = FALSE
    WriterBound = FALSE
    FixHoldWriter = FALSE
SPECIFICATION Spec
INVARIANTS InlineNewest
CHECK_DEADLOCK FALSE
