\* expect: pass
\* module: DispatchPipeline
\* Today, pi awaiting each tool_result handler (sequential tools, no abandonment): one pipeline at a time, so the inline-blocker record ends on the newest revision.
CONSTANTS
    Edits = 3
    Parallel = FALSE
    Orphan = FALSE
    ImmediateEdits = {}
    ClaimGap = FALSE
    InFlightDedupe = TRUE
    WidgetGuard = TRUE
    FixRecord = FALSE
    FixClear = FALSE
    FixTomb = FALSE
    FixQueue = FALSE
    FixQueueRefresh = FALSE
    FixReToken = FALSE
SPECIFICATION Spec
INVARIANTS InlineNewest
CHECK_DEADLOCK FALSE
