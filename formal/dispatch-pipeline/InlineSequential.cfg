\* expect: pass
\* module: DispatchPipeline
\* Without the inline guard (the code before #3507), pi awaiting each tool_result handler (sequential tools, no abandonment): one pipeline at a time, so the record ends on the newest revision.
CONSTANTS
    Edits = 3
    Parallel = FALSE
    Orphan = FALSE
    ImmediateEdits = {}
    ClaimGap = FALSE
    InFlightDedupe = TRUE
    WidgetGuard = TRUE
    FixRecord = FALSE
    FixClear = FALSE
    FixTomb = FALSE
    FixQueue = FALSE
    FixQueueRefresh = FALSE
    FixReToken = FALSE
    WriterBound = FALSE
    FixHoldWriter = FALSE
SPECIFICATION Spec
INVARIANTS InlineNewest
CHECK_DEADLOCK FALSE
