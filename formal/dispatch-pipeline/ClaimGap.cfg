\* expect: pass
\* module: DispatchPipeline
\* Fixed code (#3508), non-resident clients: the classified path awaits the bootstrap clients ABOVE claimPipelineDispatch, as the observed path does, so claim and registration stay one synchronous block.
CONSTANTS
    Edits = 2
    Parallel = TRUE
    Orphan = FALSE
    ImmediateEdits = {}
    ClaimGap = FALSE
    InFlightDedupe = TRUE
    WidgetGuard = TRUE
    FixRecord = FALSE
    FixClear = FALSE
    FixTomb = FALSE
    FixQueue = FALSE
    FixQueueRefresh = FALSE
    FixReToken = FALSE
SPECIFICATION Spec
INVARIANTS NoDoubleDispatch
CHECK_DEADLOCK FALSE
