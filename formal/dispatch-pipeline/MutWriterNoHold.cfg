\* expect: violated NoLostEdit
\* module: DispatchPipeline
\* Mutant of FixerOrphan (the round-1 code): the writer's own bound gives up on the in-place child, the pipeline releases the hold, pi's next edit lands, and the child writes its fix of the pre-edit bytes over it.
CONSTANTS
    Edits = 2
    Parallel = FALSE
    Orphan = TRUE
    ImmediateEdits = {1}
    ClaimGap = FALSE
    InFlightDedupe = TRUE
    WidgetGuard = TRUE
    FixRecord = FALSE
    FixClear = FALSE
    FixTomb = FALSE
    FixQueue = TRUE
    FixQueueRefresh = TRUE
    FixReToken = TRUE
    WriterBound = TRUE
    FixHoldWriter = FALSE
SPECIFICATION Spec
INVARIANTS NoLostEdit
CHECK_DEADLOCK FALSE
