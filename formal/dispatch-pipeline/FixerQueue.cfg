\* expect: pass
\* module: DispatchPipeline
\* Candidate fix: the fixer runs inside withFileMutationQueue(F) from its read through the after-read, refresh and postWriteStateHash; parallel tools and abandonment, two of three edits immediate.
CONSTANTS
    Edits = 3
    Parallel = TRUE
    Orphan = TRUE
    ImmediateEdits = {1,2}
    ClaimGap = FALSE
    InFlightDedupe = TRUE
    WidgetGuard = TRUE
    FixRecord = FALSE
    FixClear = FALSE
    FixTomb = FALSE
    FixQueue = TRUE
    FixQueueRefresh = TRUE
    FixReToken = TRUE
SPECIFICATION Spec
INVARIANTS NoLostEdit NoForeignAttribution WidgetExact
CHECK_DEADLOCK FALSE
