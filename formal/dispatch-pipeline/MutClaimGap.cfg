\* expect: violated NoDoubleDispatch
\* module: DispatchPipeline
\* Mutant of ClaimGap (the code before #3508): with non-resident clients the classified path awaits the bootstrap clients between claim and registration, so two handlers for one post-write state both proceed.
CONSTANTS
    Edits = 2
    Parallel = TRUE
    Orphan = FALSE
    ImmediateEdits = {}
    ClaimGap = TRUE
    InFlightDedupe = TRUE
    WidgetGuard = TRUE
    FixRecord = FALSE
    FixClear = FALSE
    FixTomb = FALSE
    FixQueue = FALSE
    FixQueueRefresh = FALSE
    FixReToken = FALSE
SPECIFICATION Spec
INVARIANTS NoDoubleDispatch
CHECK_DEADLOCK FALSE
