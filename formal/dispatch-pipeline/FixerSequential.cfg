\* expect: pass
\* module: DispatchPipeline
\* Without the queue (the code before #3506), sequential tools and no abandonment: the first write's immediate autofix finishes before pi starts the next edit, so pi's own serialisation hid the bug.
CONSTANTS
    Edits = 3
    Parallel = FALSE
    Orphan = FALSE
    ImmediateEdits = {1}
    ClaimGap = FALSE
    InFlightDedupe = TRUE
    WidgetGuard = TRUE
    FixRecord = FALSE
    FixClear = FALSE
    FixTomb = FALSE
    FixQueue = FALSE
    FixQueueRefresh = FALSE
    FixReToken = FALSE
SPECIFICATION Spec
INVARIANTS NoLostEdit NoForeignAttribution WidgetExact
CHECK_DEADLOCK FALSE
