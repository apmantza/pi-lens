\* expect: pass
\* module: DispatchPipeline
\* Fixed code (#3506), parallel tools: the first write's in-place autofix runs inside withFileMutationQueue(F) from its read through the refresh and postWriteStateHash, and a refresh that moved the bytes takes a fresh writeIndex.
CONSTANTS
    Edits = 2
    Parallel = TRUE
    Orphan = FALSE
    ImmediateEdits = {1}
    ClaimGap = FALSE
    InFlightDedupe = TRUE
    WidgetGuard = TRUE
    FixRecord = FALSE
    FixClear = FALSE
    FixTomb = FALSE
    FixQueue = TRUE
    FixQueueRefresh = TRUE
    FixReToken = TRUE
SPECIFICATION Spec
INVARIANTS NoLostEdit
CHECK_DEADLOCK FALSE
