\* expect: pass
\* module: DispatchPipeline
\* Fixed code (#3506), parallel tools: the first write's in-place autofix runs inside withFileMutationQueue(F) from its read through the refresh and postWriteStateHash, and a refresh that moved the bytes takes a fresh writeIndex; a writer its own bound gave up on keeps the hold until it writes (review round 1).
CONSTANTS
    Edits = 2
    Parallel = TRUE
    Orphan = FALSE
    ImmediateEdits = {1}
    ClaimGap = FALSE
    InFlightDedupe = TRUE
    WidgetGuard = TRUE
    FixRecord = FALSE
    FixClear = FALSE
    FixTomb = FALSE
    FixQueue = TRUE
    FixQueueRefresh = TRUE
    FixReToken = TRUE
    WriterBound = TRUE
    FixHoldWriter = TRUE
SPECIFICATION Spec
INVARIANTS NoLostEdit
CHECK_DEADLOCK FALSE
