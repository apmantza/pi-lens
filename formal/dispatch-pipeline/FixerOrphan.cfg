\* expect: pass
\* module: DispatchPipeline
\* Fixed code (#3506), sequential tools: the handler's 10 s bound abandons the pipeline and pi starts the next edit, which queues behind the orphaned autofix instead of being overwritten by it.
CONSTANTS
    Edits = 2
    Parallel = FALSE
    Orphan = TRUE
    ImmediateEdits = {1}
    ClaimGap = FALSE
    InFlightDedupe = TRUE
    WidgetGuard = TRUE
    FixRecord = FALSE
    FixClear = FALSE
    FixTomb = FALSE
    FixQueue = TRUE
    FixQueueRefresh = TRUE
    FixReToken = TRUE
SPECIFICATION Spec
INVARIANTS NoLostEdit
CHECK_DEADLOCK FALSE
