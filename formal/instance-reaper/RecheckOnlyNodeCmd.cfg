\* expect: pass
\* module: InstanceReaper
\* #3538, fixed code, recorded command "node" (the Windows .ps1 bypass,
\* launch.ts). With the re-check alone this violated NoWrongKill: a live pi-lens
\* host on the reused pid matched and was killed.
CONSTANTS
    Pids = {0, 1, 2, 3}
    Insts = {A, B}
    Mcp = {}
    Crashable = {A}
    NoKids = {B}
    MaxBirth = 5
    Reapers = {r1}
    Backstops = {}
    Posix = FALSE
    PidReuse = TRUE
    OtherProcs = FALSE
    RecCmd = "node"
    LateRecord = FALSE
    FootprintPrune = FALSE
    ScanFail = FALSE
    StalePrune = FALSE
    Guards = {"parentDead", "identity", "ppid", "tracked", "lock"}
    FixParts = {"birth", "recheck", "tag"}
SPECIFICATION Spec
INVARIANTS TypeOK NoWrongKill
CHECK_DEADLOCK FALSE
