\* expect: violated OrphanReachable
\* module: InstanceReaper
\* Alternative to the tag: keep child records until they are verified gone.
\* A crash before the record lands still leaks on POSIX.
CONSTANTS
    Pids = {0, 1, 2, 3}
    Insts = {A, B}
    Mcp = {}
    Crashable = {A, B}
    NoKids = {}
    MaxBirth = 4
    Reapers = {r1}
    Backstops = {b1}
    Posix = TRUE
    PidReuse = TRUE
    OtherProcs = FALSE
    RecCmd = "ts"
    LateRecord = TRUE
    FootprintPrune = TRUE
    ScanFail = TRUE
    StalePrune = TRUE
    Guards = {"parentDead", "identity", "ppid", "tracked", "lock"}
    FixParts = {"birth", "recheck", "retain"}
SPECIFICATION Spec
INVARIANTS TypeOK OrphanReachable
CHECK_DEADLOCK FALSE
