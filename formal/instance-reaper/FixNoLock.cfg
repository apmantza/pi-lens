\* expect: pass
\* module: InstanceReaper
\* Guard mutant of FixSafety: two backstops and no backstop lock. Passes: the
\* lock saves cost; it does not carry kill safety.
CONSTANTS
    Pids = {0, 1, 2, 3}
    Insts = {A, B}
    Mcp = {}
    Crashable = {A, B}
    NoKids = {}
    MaxBirth = 4
    Reapers = {r1}
    Backstops = {b1, b2}
    Posix = FALSE
    PidReuse = TRUE
    OtherProcs = TRUE
    RecCmd = "node"
    LateRecord = TRUE
    FootprintPrune = FALSE
    ScanFail = FALSE
    StalePrune = FALSE
    Guards = {"parentDead", "identity", "ppid", "tracked"}
    FixParts = {"birth", "recheck", "tag"}
SPECIFICATION Spec
INVARIANTS TypeOK NoWrongKill
CHECK_DEADLOCK FALSE
