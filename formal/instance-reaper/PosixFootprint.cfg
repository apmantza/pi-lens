\* expect: pass
\* module: InstanceReaper
\* #3539, fixed code on POSIX. Before the fix this violated OrphanReachable:
\* pilens_health pruned a crashed instance's entry without killing its children,
\* and the backstop read the reparented ppid as alive.
CONSTANTS
    Pids = {0, 1, 2, 3}
    Insts = {A, B}
    Mcp = {}
    Crashable = {A}
    NoKids = {}
    MaxBirth = 5
    Reapers = {r1}
    Backstops = {b1}
    Posix = TRUE
    PidReuse = FALSE
    OtherProcs = FALSE
    RecCmd = "ts"
    LateRecord = FALSE
    FootprintPrune = TRUE
    ScanFail = FALSE
    StalePrune = FALSE
    Guards = {"parentDead", "identity", "ppid", "tracked", "lock"}
    FixParts = {"birth", "recheck", "tag"}
SPECIFICATION Spec
INVARIANTS TypeOK OrphanReachable
CHECK_DEADLOCK FALSE
