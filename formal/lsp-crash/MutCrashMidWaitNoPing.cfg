\* expect: violated NoFalseClean
\* module: LspCrash
\* Mutant: the silent-clean confirm without its pingLiveness() precondition (#1277).
CONSTANTS
    Shape = "C"
    Concurrent = FALSE
    Silent = TRUE
    MaxCrashes = 1
    Uptimes = {"early","mid","long"}
    MaxEvicts = 0
    LeaseCheck = TRUE
    PingGuard = FALSE
    WaitTimeout = TRUE
    FastPath = TRUE
    WindowTrip = TRUE
    Trip = 3
    ClearReadyOnDeath = TRUE
    ReadyGuard = TRUE
    Fix = "bind"
SPECIFICATION Spec
INVARIANTS TypeOK NoFalseClean
CHECK_DEADLOCK FALSE
