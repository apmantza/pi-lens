\* expect: violated NoFalseClean
\* module: LspCrash
\* Alternative fix plus a dead client's notify resolving false: a write that landed before the crash still marks after the clear.
CONSTANTS
    Shape = "SCC"
    Concurrent = TRUE
    Silent = TRUE
    MaxCrashes = 1
    Uptimes = {"early","mid","long"}
    MaxEvicts = 0
    LeaseCheck = TRUE
    PingGuard = TRUE
    WaitTimeout = TRUE
    FastPath = TRUE
    WindowTrip = TRUE
    Trip = 3
    ClearReadyOnDeath = TRUE
    ColdGuard = TRUE
    ReadyGuard = TRUE
    Fix = "clearDeadFalse"
SPECIFICATION Spec
INVARIANTS TypeOK NoFalseClean
CHECK_DEADLOCK FALSE
