\* expect: violated SkipImpliesHeld
\* module: LspCrash
\* Mutant, the pre-#3501 code, any server: the respawned client is never sent the document while recentTouches says it has it.
CONSTANTS
    Shape = "SC"
    Concurrent = FALSE
    Silent = FALSE
    MaxCrashes = 1
    Uptimes = {"early","mid","long"}
    MaxEvicts = 0
    LeaseCheck = TRUE
    PingGuard = TRUE
    WaitTimeout = TRUE
    FastPath = TRUE
    WindowTrip = TRUE
    Trip = 3
    ClearReadyOnDeath = TRUE
    ReadyGuard = TRUE
    Fix = "none"
SPECIFICATION Spec
INVARIANTS TypeOK SkipImpliesHeld
CHECK_DEADLOCK FALSE
