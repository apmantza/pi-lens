\* expect: pass
\* module: LspCrash
\* Code (#3502 verify round 3): a warm-up that finds no client (the key in its breaker cooldown) caches the key cold, and the client that later registers forgets that verdict.
CONSTANTS
    Shape = "CWC"
    Concurrent = FALSE
    Silent = FALSE
    MaxCrashes = 1
    Uptimes = {"early"}
    MaxEvicts = 0
    LeaseCheck = TRUE
    PingGuard = TRUE
    WaitTimeout = TRUE
    FastPath = TRUE
    WindowTrip = TRUE
    Trip = 3
    ClearReadyOnDeath = TRUE
    ColdGuard = TRUE
    RegClear = TRUE
    ReadyGuard = TRUE
    Fix = "bind"
SPECIFICATION Spec
INVARIANTS TypeOK ColdIsCurrent ReadyIsCurrent
CHECK_DEADLOCK FALSE
