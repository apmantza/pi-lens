\* expect: violated NoPhantomAfterRename
\* module: DocumentLifecycle
\* Today (#3477): a change queued before the rename runs after the close and re-opens the old path with a fallback didOpen (phantom invariant only; the in-flight race is RenameOpenDocument.cfg).
CONSTANTS
    Touches = 1
    TouchAfterRename = TRUE
    StartOpen = TRUE
    QueuedClose = FALSE
SPECIFICATION Spec
INVARIANTS NoPhantomAfterRename
CHECK_DEADLOCK FALSE
