\* expect: violated NoPhantomAfterRename
\* module: DocumentLifecycle
\* Today (#3477): rename starts while the fallback didOpen is in flight; isDocumentOpen is false, so nothing closes it.
CONSTANTS
    Touches = 1
    TouchAfterRename = FALSE
    StartOpen = FALSE
    QueuedClose = FALSE
SPECIFICATION Spec
INVARIANTS LifecycleOrder NoPhantomAfterRename
CHECK_DEADLOCK FALSE
