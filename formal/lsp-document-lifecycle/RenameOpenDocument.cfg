\* expect: violated LifecycleOrder
\* module: DocumentLifecycle
\* Today (#3477): a queued change runs while rename's didClose is in flight, and sends didChange after it.
CONSTANTS
    Touches = 1
    TouchAfterRename = FALSE
    StartOpen = TRUE
    QueuedClose = FALSE
SPECIFICATION Spec
INVARIANTS LifecycleOrder NoPhantomAfterRename
CHECK_DEADLOCK FALSE
