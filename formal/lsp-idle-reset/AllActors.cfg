\* expect: violated NoSilentLoss
\* module: IdleReset
\* Sweep, cascade, warm attach and reaping (sweep in range) together. (#3483)
CONSTANTS
    Sweep = TRUE
    SweepOverrun = FALSE
    Reaping = TRUE
    Cascade = TRUE
    WarmAttach = TRUE
    HoldCheck = TRUE
    FixParts = {}
SPECIFICATION Spec
INVARIANTS TypeOK NoResetUnderHold NoResetDuringSweep HonestSweepLabel NoSilentLoss
CHECK_DEADLOCK FALSE
