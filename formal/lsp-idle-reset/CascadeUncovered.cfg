\* expect: violated NoSilentLoss
\* module: IdleReset
\* computeCascadeForFile takes no hold; a neighbour touch that sees checkDestroyed() is dropped (if (!rawDiags) return undefined). (#3483)
CONSTANTS
    Sweep = FALSE
    SweepOverrun = FALSE
    Reaping = FALSE
    Cascade = TRUE
    WarmAttach = FALSE
    HoldCheck = TRUE
    FixParts = {}
SPECIFICATION Spec
INVARIANTS TypeOK NoResetUnderHold NoResetDuringSweep HonestSweepLabel NoSilentLoss
CHECK_DEADLOCK FALSE
