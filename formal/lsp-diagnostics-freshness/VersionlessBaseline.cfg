\* expect: violated FreshResult
\* module: DiagnosticsFreshness
\* Version-less server: a publish for the pre-touch content lands after the clear and settles the minVersion wait (isSupersededPush ~2479). (#3484)
CONSTANTS
    VersionedServer = FALSE
    SkippedWait = FALSE
    SeedFirstPush = FALSE
    AsyncServer = FALSE
    Preserve = FALSE
    Fence = FALSE
    MaxPubs = 3
    Mutant = "none"
SPECIFICATION Spec
INVARIANTS FreshResult
CHECK_DEADLOCK FALSE
