\* expect: violated InProcessLatestWins
\* module: SnapshotPromotion
\* Mutant: session_start clears the generation map and the active slot while a session-1 request is still in the worker (shape 17/22). The code keeps the coordinator process-lifetime.
CONSTANTS
    Procs = {p1}
    MaxSeq = 3
    MaxSaves = 3
    GenGate = TRUE
    SingleActive = TRUE
    SweepLiveness = FALSE
    PromoteCAS = FALSE
    AdmissionCAS = FALSE
    CASAtomic = FALSE
    AllowCrash = FALSE
    SessionReset = TRUE
    RefuseAll = FALSE
SPECIFICATION Spec
INVARIANTS TypeOK InProcessLatestWins
CHECK_DEADLOCK FALSE
