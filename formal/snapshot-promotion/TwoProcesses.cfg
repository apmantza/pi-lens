\* expect: violated NoRegression
\* module: SnapshotPromotion
\* Today's code, two processes on one project (pi + MCP server): a slow worker's older view is renamed over a sibling's newer body.
CONSTANTS
    Procs = {p1, p2}
    MaxSeq = 2
    MaxSaves = 1
    GenGate = TRUE
    SingleActive = TRUE
    SweepLiveness = FALSE
    PromoteCAS = FALSE
    AdmissionCAS = FALSE
    CASAtomic = FALSE
    AllowCrash = FALSE
    SessionReset = FALSE
SPECIFICATION Spec
INVARIANTS TypeOK NoRegression
CHECK_DEADLOCK FALSE
