\* expect: pass
\* module: SessionStraddle
\* Shipped code (#3512 r1): the session-1 handler can admit its compute after the reset (index.ts abandons it at its bound). The admission drops on the generation captured at dispatch; below the cap.
CONSTANTS
    QuietWindow = TRUE
    ResetClears = TRUE
    Dedupe = TRUE
    ToolDrift = FALSE
    Strays = TRUE
    Overflow = FALSE
    LateAdmit = TRUE
    FixParts = {"settle","reconcile","reconcileTaskCapture","admission","stray"}
SPECIFICATION Spec
INVARIANTS TypeOK NoCrossSessionState NoCrossSessionDelivery NoDropFreshTouch NoDropFreshAdmission
CHECK_DEADLOCK FALSE
