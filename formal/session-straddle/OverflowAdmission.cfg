\* expect: pass
\* module: SessionStraddle
\* Shipped code (#3512): the session-1 compute was admitted past the 32-compute cap and also strays; session 2 admits its own past the cap. Nothing of session 1's is delivered in session 2 and nothing of session 2's is dropped.
CONSTANTS
    QuietWindow = TRUE
    ResetClears = TRUE
    Dedupe = TRUE
    ToolDrift = FALSE
    Strays = TRUE
    Overflow = TRUE
    LateAdmit = FALSE
    FixParts = {"settle","reconcile","reconcileTaskCapture","admission","stray"}
SPECIFICATION Spec
INVARIANTS TypeOK NoCrossSessionState NoCrossSessionDelivery NoDropFreshTouch NoDropFreshAdmission
CHECK_DEADLOCK FALSE
