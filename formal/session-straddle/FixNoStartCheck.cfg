\* expect: violated NoDropFreshTouch
\* module: SessionStraddle
\* Fix mutant (#3499 round 1): no start check. A stale reconcile drains session 2's own touch and its append guard drops it.
CONSTANTS
    QuietWindow = TRUE
    ResetClears = TRUE
    Dedupe = TRUE
    ToolDrift = FALSE
    Strays = FALSE
    FixParts = {"settle","reconcile"}
SPECIFICATION Spec
INVARIANTS TypeOK NoCrossSessionState NoDropFreshTouch
CHECK_DEADLOCK FALSE
