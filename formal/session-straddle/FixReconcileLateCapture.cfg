\* expect: violated NoCrossSessionState
\* module: SessionStraddle
\* Fix mutant: the reconcile guard captures the generation when its task starts, after the settle task, not when the quiet window starts.
CONSTANTS
    QuietWindow = TRUE
    ResetClears = TRUE
    Dedupe = TRUE
    ToolDrift = FALSE
    FixParts = {"settle","reconcileLate"}
SPECIFICATION Spec
INVARIANTS TypeOK NoCrossSessionState NoCrossSessionDelivery
CHECK_DEADLOCK FALSE
