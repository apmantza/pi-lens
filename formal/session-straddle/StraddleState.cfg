\* expect: pass
\* module: SessionStraddle
\* Shipped code (#3499): session 1's quiet window is still settling when session 2's reset runs; its late settle and reconcile writes drop.
CONSTANTS
    QuietWindow = TRUE
    ResetClears = TRUE
    Dedupe = TRUE
    ToolDrift = FALSE
    FixParts = {"settle","reconcile"}
SPECIFICATION Spec
INVARIANTS TypeOK NoCrossSessionState
CHECK_DEADLOCK FALSE
