\* expect: violated NoCrossSessionState
\* module: SessionStraddle
\* Fix mutant: only the reconcile append is guarded.
CONSTANTS
    QuietWindow = TRUE
    ResetClears = TRUE
    Dedupe = TRUE
    ToolDrift = FALSE
    FixParts = {"reconcile"}
SPECIFICATION Spec
INVARIANTS TypeOK NoCrossSessionState NoCrossSessionDelivery
CHECK_DEADLOCK FALSE
