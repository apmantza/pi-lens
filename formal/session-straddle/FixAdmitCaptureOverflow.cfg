\* expect: violated NoCrossSessionState
\* module: SessionStraddle
\* Capture mutant (#3512 r0, the review's Admit1 probe): past the cap, the late admission captures session 2's generation, so its .then appends session 1's run into session 2.
CONSTANTS
    QuietWindow = TRUE
    ResetClears = TRUE
    Dedupe = TRUE
    ToolDrift = FALSE
    Strays = TRUE
    Overflow = TRUE
    LateAdmit = TRUE
    FixParts = {"settle","reconcile","reconcileTaskCapture","admission","stray","admissionAtAdmit"}
SPECIFICATION Spec
INVARIANTS TypeOK NoCrossSessionState NoCrossSessionDelivery
CHECK_DEADLOCK FALSE
