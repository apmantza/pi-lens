\* expect: violated NoCrossSessionDelivery
\* module: SessionStraddle
\* Capture mutant (#3512): the touch is stamped with the generation current when it is recorded, the design the issue rules out. A stray recorded after the reset reads session 2's generation and is delivered.
CONSTANTS
    QuietWindow = TRUE
    ResetClears = TRUE
    Dedupe = TRUE
    ToolDrift = FALSE
    Strays = TRUE
    Overflow = FALSE
    FixParts = {"settle","reconcile","reconcileTaskCapture","admission","stray","strayRecordCapture"}
SPECIFICATION Spec
INVARIANTS TypeOK NoCrossSessionDelivery NoDropFreshTouch
CHECK_DEADLOCK FALSE
