\* expect: violated NoDropFreshTouch
\* module: SessionStraddle
\* Capture mutant (#3512): one dispatch handle, session 1's, is reused for later dispatches. Session 2's own touch is dropped at its record (shape 54).
CONSTANTS
    QuietWindow = TRUE
    ResetClears = TRUE
    Dedupe = TRUE
    ToolDrift = FALSE
    Strays = TRUE
    Overflow = FALSE
    LateAdmit = FALSE
    FixParts = {"settle","reconcile","reconcileTaskCapture","admission","stray","dispatchHoist"}
SPECIFICATION Spec
INVARIANTS TypeOK NoCrossSessionDelivery NoDropFreshTouch
CHECK_DEADLOCK FALSE
