\* expect: violated NoCrossSessionState
\* module: SessionStraddle
\* Capture mutant (#3512 r0): the admission captures when it admits, after the pipeline await. A handler resuming after the reset parks session 1's compute in session 2.
CONSTANTS
    QuietWindow = TRUE
    ResetClears = TRUE
    Dedupe = TRUE
    ToolDrift = FALSE
    Strays = TRUE
    Overflow = FALSE
    LateAdmit = TRUE
    FixParts = {"settle","reconcile","reconcileTaskCapture","admission","stray","admissionAtAdmit"}
SPECIFICATION Spec
INVARIANTS TypeOK NoCrossSessionState NoCrossSessionDelivery
CHECK_DEADLOCK FALSE
