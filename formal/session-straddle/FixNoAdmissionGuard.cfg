\* expect: violated NoCrossSessionDelivery
\* module: SessionStraddle
\* Guard mutant (#3512): the overflow append is unguarded, the code before #3512. The detached .then appends session 1's run after the reset and session 2's turn_end delivers it.
CONSTANTS
    QuietWindow = TRUE
    ResetClears = TRUE
    Dedupe = TRUE
    ToolDrift = FALSE
    Strays = FALSE
    Overflow = TRUE
    LateAdmit = FALSE
    FixParts = {"settle","reconcile","reconcileTaskCapture","stray"}
SPECIFICATION Spec
INVARIANTS TypeOK NoCrossSessionDelivery NoDropFreshAdmission
CHECK_DEADLOCK FALSE
