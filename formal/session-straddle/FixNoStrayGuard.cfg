\* expect: violated NoCrossSessionDelivery
\* module: SessionStraddle
\* Guard mutant (#3512): the tier-3 touch record is unguarded, the code before #3512. Session 2's window reconciles the stray and delivers it.
CONSTANTS
    QuietWindow = TRUE
    ResetClears = TRUE
    Dedupe = TRUE
    ToolDrift = FALSE
    Strays = TRUE
    Overflow = FALSE
    LateAdmit = FALSE
    FixParts = {"settle","reconcile","reconcileTaskCapture","admission"}
SPECIFICATION Spec
INVARIANTS TypeOK NoCrossSessionDelivery NoDropFreshTouch
CHECK_DEADLOCK FALSE
