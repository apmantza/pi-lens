\* expect: pass
\* module: SessionStraddle
\* Shipped code (#3512): a session-1 compute records a touch after the reset; the touch record drops it on the generation its dispatch captured, and session 2's own touch is delivered.
CONSTANTS
    QuietWindow = TRUE
    ResetClears = TRUE
    Dedupe = TRUE
    ToolDrift = FALSE
    Strays = TRUE
    Overflow = FALSE
    LateAdmit = FALSE
    FixParts = {"settle","reconcile","reconcileTaskCapture","admission","stray"}
SPECIFICATION Spec
INVARIANTS TypeOK NoCrossSessionState NoCrossSessionDelivery NoDropFreshTouch
CHECK_DEADLOCK FALSE
