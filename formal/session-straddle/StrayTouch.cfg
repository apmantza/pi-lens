\* expect: violated NoCrossSessionDelivery
\* module: SessionStraddle
\* Residual (#3512): a session-1 compute records a touch after the reset. The shipped code cannot tell it from session 2's own, and session 2's window delivers it.
CONSTANTS
    QuietWindow = TRUE
    ResetClears = TRUE
    Dedupe = TRUE
    ToolDrift = FALSE
    Strays = TRUE
    FixParts = {"settle","reconcile","reconcileStart"}
SPECIFICATION Spec
INVARIANTS TypeOK NoCrossSessionDelivery
CHECK_DEADLOCK FALSE
