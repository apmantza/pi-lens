\* expect: violated NoStaleFindings
\* module: LateAuxDrain
\* Today: an external edit lands inside the aux-grace wait; markedAtMs is Date.now() after the wait (index.ts ~6028), so the edit's mtime predates the baseline. (#3482)
CONSTANTS
    AgentTouches = 1
    ExternalEdits = 1
    MaxDrains = 3
    MaxRearms = 2
    Tol = 0
    AllowDefer = FALSE
    AllowCancel = FALSE
    PublishedAtCheck = TRUE
    MtimeGate = TRUE
    RefreshOnStale = TRUE
    CountBind = FALSE
    MarkAtNotify = FALSE
    ExtDuringGrace = TRUE
SPECIFICATION Spec
INVARIANTS NoStaleFindings
CHECK_DEADLOCK FALSE
