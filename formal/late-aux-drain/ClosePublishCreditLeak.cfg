\* expect: violated NoExcessCloseCredit
\* module: LateAuxDrain
\* #3548 review r1 B1 before the fix: a skip credit granted by one close, never spent because its own close-triggered publish never arrived before the reopen, survives into a SECOND close and stacks with its fresh credit — two credits outstanding for a lifetime that ever legitimately owes one at a time, wrongly skipping that close's first genuine backlog publish too (permanently withheld — NoFreshWithheld also reds here, but so does the unrelated, already-accepted single-close ordering race; NoExcessCloseCredit is the leak's own, uncontaminated signature).
CONSTANTS
    AgentTouches = 3
    ExternalEdits = 0
    MaxDrains = 5
    MaxRearms = 2
    Tol = 0
    AllowDefer = FALSE
    AllowCancel = FALSE
    PublishedAtCheck = TRUE
    MtimeGate = TRUE
    RefreshOnStale = FALSE
    CountBind = TRUE
    MarkAtNotify = TRUE
    ExtDuringGrace = FALSE
    Refresh = "none"
    RefreshRebaseline = TRUE
    RefreshOvertake = FALSE
    AllowSave = FALSE
    SaveExpect = 1
    Closes = 2
    Carry = "span"
    AllowClosePublish = TRUE
    ClosePublishCounted = FALSE
    ResetClosePublishCredit = FALSE
SPECIFICATION Spec
INVARIANTS NoExcessCloseCredit
CHECK_DEADLOCK FALSE
