\* expect: violated NoStaleFindings
\* module: LateAuxDrain
\* #3548 before the fix: a publishesOnClose server's own close-triggered publish (typos' did_close) is credited as the closed lifetime's owed scan, freeing an in-flight scan's later (stale) answer to be taken as the reopened file's fresh answer.
CONSTANTS
    AgentTouches = 3
    ExternalEdits = 0
    MaxDrains = 4
    MaxRearms = 2
    Tol = 0
    AllowDefer = TRUE
    AllowCancel = FALSE
    PublishedAtCheck = TRUE
    MtimeGate = TRUE
    RefreshOnStale = FALSE
    CountBind = TRUE
    MarkAtNotify = TRUE
    ExtDuringGrace = FALSE
    Refresh = "none"
    RefreshRebaseline = TRUE
    RefreshOvertake = FALSE
    AllowSave = FALSE
    SaveExpect = 1
    Closes = 1
    Carry = "span"
    AllowClosePublish = TRUE
    ClosePublishCounted = TRUE
    ResetClosePublishCredit = FALSE
SPECIFICATION Spec
INVARIANTS NoStaleFindings
CHECK_DEADLOCK FALSE
