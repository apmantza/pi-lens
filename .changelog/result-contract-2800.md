### Tool result contract

Use one renderer for pi tool results and the MCP mirror, including result status,
diagnostic severity, and usage lines. A both-surfaces governance test drives the
real handlers for every paired registry tool.
