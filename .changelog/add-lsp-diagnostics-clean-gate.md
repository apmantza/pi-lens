---
section: Added
---

- **Gate nightly LSP smoke rows on primary diagnostics** (refs #2780) — the tool-smoke workflow now drives each installed server through the real `lens_diagnostics` handler and fails when a known-error fixture produces no primary finding.
