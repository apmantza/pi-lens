---
section: Fixed
---

- **Preserve pi situational-tool observations across pi session replacements and emit once on replacement.** Resume and fork preserve the conversation's row; reload re-runs the extension factory and emits the prior runtime row before opening a new set. (refs #2858)
