---
section: Fixed
---

- **Register delivered bash and native read ranges (closes #2802)** — Read-guard registration now uses the lines the host returned, including output-cap and EOF-clamped reads.
